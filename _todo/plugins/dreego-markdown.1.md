---
area: plugins
phase: planned-v0.3
---
# Markdown plugin

Implement the first external body-language processor for
`<body lang="md">` without adding a parser dependency to core. Dreego retains
ownership of component composition, control flow, expressions, escaping, and
the single body root. See `_plan/v0.3-language-processors.md`.
