---
area: plugins
phase: future
---
# Key-value store plugins

Explore local and remote key-value stores with explicit missing-value, expiry, concurrency, and namespace semantics. Keep their APIs plugin-owned until multiple implementations prove a shared contract.
