---
area: plugins
phase: future
---
# Mail plugin

Provide SMTP and HTTP-provider adapters for transactional email. The plugin owns
its initial API; a provider-neutral shared interface is evaluated only after
multiple implementations expose a proven common contract.
