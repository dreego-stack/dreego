services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        DREEGO_PORT: ${DREEGO_PORT:-8080}
    ports:
      - "${DREEGO_PORT:-8080}:${DREEGO_PORT:-8080}"
    environment:
      - DREEGO_PORT=${DREEGO_PORT:-8080}
    restart: unless-stopped
