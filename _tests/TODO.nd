# Test TODO — v0.0.19

Tests identified by 10 subagent audits, filtered for v0.0.19 relevance.
Removed: Golden Tests (no testdata/ infra), CSRF/Session (already covered), test-cmd (no subcommand), B7/B9/B10 (fixed or package missing).

## Bugs to Fix (before tests)

| # | Bug | File:Line |
|---|-----|-----------|
| — | discovered: `{#else if}` not supported | core/lexer_brace.go, core/parser_section_div.go, core/codegen.go, core/codegen_template.go |
| — | discovered: `import` inside `<go>` blocks not preserved for package-level declarations | core/codegen.go splitGoSections |
