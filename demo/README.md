# Demo applications

- [`demo-ssr`](demo-ssr/) is the server-side rendering reference application.
- [`demo-wailsv3`](demo-wailsv3/) is the experimental Wails v3 desktop reference application.

Each demo is an independent Go module with its own `main.go`.
